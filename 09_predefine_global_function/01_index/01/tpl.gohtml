<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>predefined function</title>
    </head>
    <body>
    {{index . 1}}
    {{index . 0}}
    {{index . 2}}

    <!-- 
    index 
    a func you can use in a template
    it is a predefined global function

    Index returns the result of indexing its argument by the
    following argument. Thus "index x 1 2 3" is, in Go syntax,
    x[1][2][3]. Each index item must be a map, slice or array.

    https://godoc.org/text/template#hdr-Functions

    -->

    </body>
</html>